# This is an example of a smoke test workflow

name: Smoke Tests

on: [push]

jobs:
  smoke-tests:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v2

    - name: Run Smoke Tests
      run: |
        npm install
        npm run smoke-tests    
