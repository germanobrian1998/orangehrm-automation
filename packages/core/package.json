{
  "name": "@qa-framework/core",
  "version": "1.0.0",
  "description": "Core framework base - reusable page objects, API client, logger, config and utils",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "playwright test",
    "lint": "eslint src tests --ext .ts",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "@playwright/test": "^1.40.0",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "axios": "^1.14.0",
    "dotenv": "^16.0.0",
    "winston": "^3.19.0",
    "winston-daily-rotate-file": "^5.0.0"
  }
}
