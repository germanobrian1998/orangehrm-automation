{
  "name": "@qa-framework/hrm-api-suite",
  "version": "1.0.0",
  "description": "HRM REST API test suite - request/response validation, contract testing, performance",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "playwright test",
    "test:api": "playwright test --grep @api",
    "test:mock": "USE_MOCK_API=true playwright test --grep @mock",
    "test:performance": "playwright test --grep @performance",
    "lint": "eslint src tests --ext .ts",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "@playwright/test": "^1.40.0",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.0.0",
    "msw": "^2.12.14",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@faker-js/faker": "^8.0.0",
    "@qa-framework/core": "*",
    "@qa-framework/shared-utils": "*"
  }
}
